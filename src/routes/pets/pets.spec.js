describe('petRouter', () => {
  it('should return all instances when post method on "/" is requested', () => {});
  it('should return a pet when get method on "/:id" is requested', () => {});
  it('should call petService.create when post method on "/" is requested', () => {});
  it('should update pet given an id when put method on "/:id" is requested', () => {});
})