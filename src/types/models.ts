export enum UserType {
  Professional = 'ProfessionalUser',
  PetOWner = 'PetOwnerUser',
}

export enum Gender {
  Male = 'male',
  Female = 'female',
  NoBinary = 'no binary',
}
export enum Steps {
  PersonalInformation = 'PersonalInformation',
  Pet = 'Pet',
  Completed = 'Completed',
}

export enum Size {
  Small = 'small',
  Medium = 'medium',
  Big = 'big',
}
